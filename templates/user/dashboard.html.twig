{% extends 'base.html.twig' %}

{% block title %}Novosti{% endblock %}
{% block navigation %}
    {% include 'user/navigation.html.twig' %}
{% endblock %}
{% block body %}
    {% for post in posts %}
        {% set vars = {'header': post.course_aka, 'title': post.course_name, 'text': post.post_data, 'user': post.user_name ~ post.user_lastname, 'date': post.date_updated|date('d.m.Y'), 'post_id': post.post_id, 'likes': post.post_likes|default(0), 'dislikes': post.post_dislikes|default(0), 'file': file} %}
        {% include 'user/card.html.twig' with vars %}
    {% endfor %}
{% endblock %}