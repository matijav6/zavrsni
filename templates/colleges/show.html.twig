{% extends 'base.html.twig' %}
{% block navigation %}
    {% include 'user/navigation.html.twig' %}
{% endblock %}
{% block title %}Fakultet{% endblock %}

{% block body %}
    <div class="container-fluid form-inline mb-3">
        <h1>Fakultet {{ college.name }}</h1>
    </div>

    <table class="table table-bordered table-condensed table-hover p-2">
        <tbody>
        <tr>
            <th>ID</th>
            <td>{{ college.id }}</td>
        </tr>
        <tr>
            <th>Sinonim</th>
            <td>{{ college.aka }}</td>
        </tr>
        <tr>
            <th>Naziv</th>
            <td>{{ college.name }}</td>
        </tr>
        <tr>
            <th>Županija</th>
            <td>{{ college.county.name }}</td>
        </tr>
        <tr>
            <th>Akcije</th>
            <td class="form-inline">
                <a href="{{ path('colleges_edit', {'id': college.id}) }}" class="btn btn-outline-secondary"> <i class="far fa-edit"></i></a>
                {{ include('colleges/_delete_form.html.twig') }}
            </td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('colleges_index') }}" class="btn btn-outline-secondary"><i class="fas fa-chevron-left"></i></a>

{% endblock %}
