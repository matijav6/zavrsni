{% extends 'base.html.twig' %}
{% block navigation %}
    {% include 'user/navigation.html.twig' %}
{% endblock %}
{% block title %}Ažuriranje{% endblock %}

{% block body %}
    <h1>Ažuriranje {{ posts.name }}</h1>
    <table class="table table-bordered table-condensed table-hover p-2">
        <tbody>
        <tr>
            <th>Objava</th>
            <th>Vrsta</th>
            <th>Korisnik</th>
            <th>Kolegij</th>
            <th class="text-center">Brisanje</th>
        </tr>
        <tr>
            <td>
                {{ include('posts/_form.html.twig', {'button_label': 'Ažuriraj'}) }}
            </td>
            <td>
                {{ include('posts/_delete_form.html.twig') }}
            </td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('posts_index') }}" class="btn btn-outline-secondary"><i class="fas fa-chevron-left"></i></a>
{% endblock %}
