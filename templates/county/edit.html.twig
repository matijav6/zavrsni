{% extends 'base.html.twig' %}

{% block title %}Edit County{% endblock %}

{% block body %}
    <h1>Edit {{ county.name }}</h1>
    <table class="table table-bordered table-condensed table-hover p-2">
        <tbody>
        <tr>
            <th>Naziv</th>
            <th class="text-center">Brisanje</th>
        </tr>
        <tr>
            <td>
                {{ include('county/_form.html.twig', {'button_label': 'Update'}) }}
            </td>
            <td>
                {{ include('county/_delete_form.html.twig') }}
            </td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('county_index') }}" class="btn btn-outline-secondary"><i class="fas fa-chevron-left"></i></a>
{% endblock %}
